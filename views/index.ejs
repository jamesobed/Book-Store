<!DOCTYPE html>
<html>
  <head>
    <link rel="stylesheet" href="/style.css" />
    <title>Book store</title>
    <style>
      * {
        padding: 0;
        margin: 0;
        box-sizing: border-box;
      }
      body {
        background-color: #444;
      }

      .showcase {
        width: 100%;
        height: 100vh;
        position: relative;
        background: url("./seven-shooter-hPKTYwJ4FUo-unsplash.jpg") no-repeat
          center center/cover;
      }
      .items {
        margin-top: 2rem;
        margin-bottom: 2rem;
        padding: 1rem;
        color: #fff;
        display: flex;
        justify-content: space-between;
        /* border: 2px solid white; */
        flex-wrap: wrap;
      }
      .iwe {
        list-style: none;
        padding: 2rem;
        margin: 2rem;
        border: 2px solid white;
        width: 350px;
        height: 400px;
        text-align: center;
      }
      .showcase::after {
        content: " ";
        position: absolute;
        right: 0;
        top: 0;
        width: 100%;
        height: 100%;
        background-color: rgba(0, 0, 0, 0.7);
        z-index: 1;
      }
      .menu {
        z-index: 3;
        color: white;
        position: relative;
        /* width: 85%; */
        margin: auto;
        padding: 35px;
        /* display: flex;
   align-items: center;
   justify-items: space-between;
    */
      }

      .logo {
        width: 120px;
        cursor: pointer;
      }

      .menu-nav {
        list-style: none;
        display: flex;
        float: right;
        justify-content: center;
        gap: 2rem;
      }

      li {
        font-size: 20px;
      }

      .menu-nav a {
        text-decoration: none;
        color: white;
        margin: 0 20px;
        position: relative;
        bottom: 60px;
      }

      a:hover {
        border-bottom: 4px solid green;
      }

      .content {
        width: 100%;
        position: absolute;
        top: 50%;
        transform: translateY(-50%);
        text-align: center;
        color: #fff;
        z-index: 4;
      }

      .content p {
        margin: 20px auto;
        font-weight: 100;
        line-height: 25px;
        color: rgb(240, 232, 232);
      }
    </style>
  </head>
  <body>
    <div class="wrapper">
      <header class="showcase">
        <nav class="menu">
          <h2 class="logo">BOOK STORE</h2>
          <ul class="menu-nav">
            <li><a href="/read">Home</a></li>
            <li><a href="single">Book Details</a></li>
            <li><a href="/login">Login</a></li>
            <li><a href="/register">Register</a></li>
            <li><a href="/create">Create Book</a></li>
          </ul>
        </nav>
        <hr />
        <div class="content">
          <h2 class="title">GET YOUR ARRAY INTERESTING BOOKS</h2>
          <p>E-BOOKS</p>
        </div>
      </header>
      <div class="items">
        <% for(let item of record) {%>
        <div class="book">
          <ul>
            <li class="iwe">
              <h2><%= item.name %></h2>
              <h4>serialNumber: <%= item.serialNumber %></h4>
              <br />
              <h6><%= item.id %></h6>
            </li>
          </ul>
        </div>

        <% }%>
      </div>
    </div>
    <%- include('./partials/footer.ejs') %>
  </body>
</html>
